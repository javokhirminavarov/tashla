{
  "name": "tashla",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,webapp -c blue,green \"cd api && npm run dev\" \"cd webapp && npm run dev\"",
    "build": "cd webapp && npm run build && cd ../api && npm run build",
    "start": "cd api && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
